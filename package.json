{
  "name": "camihogar-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Monorepo para la aplicación Camihogar - Frontend Next.js y Backend .NET",
  "scripts": {
    "dev": "pnpm --filter Ordina.Frontend dev",
    "build": "pnpm --filter Ordina.Frontend build",
    "lint": "pnpm --filter Ordina.Frontend lint",
    "start": "pnpm --filter Ordina.Frontend start",
    "clean": "pnpm -r exec rm -rf node_modules && rm -rf node_modules"
  },
  "devDependencies": {
    "pnpm": "^8.15.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}

