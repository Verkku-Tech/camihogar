# GitHub Container Registry Configuration
# Copia este archivo a .env y reemplaza con tus valores reales
# O ejecuta ./scripts/deploy.sh para configuración interactiva

# Tu usuario de GitHub (para GHCR) - REQUERIDO
USERNAME=tu-usuario-github

# Nombres de las imágenes (opcionales, tienen valores por defecto)
FRONTEND_IMAGE=camihogar-frontend
APIGATEWAY_IMAGE=camihogar-apigateway
SECURITY_IMAGE=camihogar-security-api
USERS_IMAGE=camihogar-users-api
PROVIDERS_IMAGE=camihogar-providers-api
ORDERS_IMAGE=camihogar-orders-api
PAYMENTS_IMAGE=camihogar-payments-api

# Token de GitHub para pull de imágenes privadas (opcional)
# Solo necesario si las imágenes son privadas
# Obtén un token en: GitHub → Settings → Developer settings → Personal access tokens
# Permisos necesarios: read:packages (mínimo) o write:packages (para CI/CD)
# GHCR_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Nota: Si tus imágenes son privadas, el script deploy.sh hará login automáticamente
# Si prefieres hacerlo manualmente:
# echo $GHCR_TOKEN | docker login ghcr.io -u $USERNAME --password-stdin
